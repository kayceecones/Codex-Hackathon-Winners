{
  "name": "codex-hackathon-winners",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview -- --host 0.0.0.0 --port $PORT"
  }
}
