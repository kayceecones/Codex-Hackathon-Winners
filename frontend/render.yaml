services:
  - type: web
    name: reason-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    previews:
      generation: automatic
